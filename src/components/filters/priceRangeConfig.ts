// Shared bounds for the marketplace price filter (integer cents).
// Used by PriceRangeSlider consumers and the active-filter-chip label so
// an unset priceMin/priceMax always displays the same open-ended bound.
export const PRICE_RANGE_MIN_CENTS = 0;
export const PRICE_RANGE_MAX_CENTS = 50000;
